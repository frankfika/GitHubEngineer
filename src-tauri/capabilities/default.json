{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the GitHub Engineer main window. The Python --serve backend exposes the IPC surface area; the Tauri shell is a thin WebView wrapper that only needs the core defaults.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
